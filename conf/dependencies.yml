self: play -> cms 2.4

require:
    - play 1.5+

    - play-codeborne -> secure 0.6
    - play-codeborne -> rebel 1.1

repositories:

    - codeborne-modules-jars:
        type: http
        artifact: https://repo.codeborne.com/play-[module]/[module]-[revision].jar
        contains:
            - play-codeborne -> secure
            - play-codeborne -> rebel
